rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary development rule: required because the current admin login is
    // session-based and is not a Firebase Authentication account.
    // Replace this rule before releasing the site publicly.
    match /orders/{orderId} {
      allow read, write: if true;
    }
  }
}
